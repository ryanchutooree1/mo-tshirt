# AGENTS.md

## Git Workflow
- After completing any requested code change, stage and commit the finished work before replying.
- Use `git add -A` and create a normal commit with a short descriptive message.
- Do not amend commits unless the user explicitly asks for it.
- If the worktree already contains unrelated changes, ask before including them in the commit.
